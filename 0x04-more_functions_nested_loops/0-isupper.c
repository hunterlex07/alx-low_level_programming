#include "main.h"

/**
 * _isupper - checks for uppercase characters.
 * @c: the charcter to be checked
 *
 * Return: 1 if character id uppercase, 0 if otherwise.
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	else
		return (o);
}
